# No extra feeds for the base-system profile.
# Third-party feeds (e.g. kenzok8/small-package) were removed because we no
# longer compile plugins into the image; install them later on the device
# with `opkg` or by adding an extra feed on the running system.
